.cards {
  width: 100%;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;

  & .card {
    flex: 1;
    opacity: 1;
    transition: opacity 500ms ease-in-out;

    &.fade-out {
      opacity: 0;
    }
  }
}
.body {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: 48px;
}

.container {
  justify-content: center;
}

